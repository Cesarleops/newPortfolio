---

const {id, title} = Astro.props
---

    <section
      class="relative bg-secondary p-5 rounded-lg sm:w-[300px] xl:h-[350px]"
      style={{ viewTransitionName: `project-${id}` }}
    >
      <a href={`${id}`}>GO</a>
      <h4 class="text-2xl text-primary">{title}</h4>
    </section>
