---

const {id, title} = Astro.props
---

    <section
      class="relative bg-primary p-5 rounded-lg sm:w-[300px] xl:h-[350px]"
      style=`view-transition-name:project-${id}`
    >
      <a href={`${id}`}>GO</a>
      <h4 class="text-2xl font-noirpro">{title}</h4>
    </section>
